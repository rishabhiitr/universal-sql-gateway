# Request Lifecycle

This document covers the complete path a query takes through the data plane.

## Component Map

Gateway → Planner → Executor → Connectors

This is a test push to verify the GitHub MCP tool is working.
